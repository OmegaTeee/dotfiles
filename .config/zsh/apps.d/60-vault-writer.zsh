# Vault Writer integrations.

export VAULT_WRITER_HOME="${HOME}/prompthub/clients/vault-writer"

alias vault-goose="${VAULT_WRITER_HOME}/vault-goose"
alias vault-aider="${VAULT_WRITER_HOME}/vault-aider"
alias vault-codex="${VAULT_WRITER_HOME}/vault-codex"
