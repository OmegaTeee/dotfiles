# Optional Zsh plugins

Drop optional plugin files in this directory. Files matching
`*.plugin.zsh` load automatically in lexical order.

Use numeric prefixes to make ordering explicit, for example:

- `10-history.plugin.zsh`
- `20-navigation.plugin.zsh`

